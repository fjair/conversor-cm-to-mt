<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ConversorMovil.Views.StartPage"
             NavigationPage.HasNavigationBar="False">
    <StackLayout BackgroundColor="#FEE689">
        <Label Text="CONVERSOR" 
               FontSize="40"
               FontAttributes="Bold"
               Margin="0,20,0,0"
               HorizontalOptions="Center"/>
        <Frame VerticalOptions="FillAndExpand"
               Margin="20"
               CornerRadius="20"
               HasShadow="False">
            <StackLayout VerticalOptions="Center"
                         Spacing="20">
                <Image Source="metro.png"></Image>
                <Label Text="(cm) a (m)"
                       FontSize="40"
                       FontAttributes="Bold"
                       HorizontalOptions="Center"/>
                <Button Text="INICIAR" 
                        BackgroundColor="#FDE080"
                        CornerRadius="10"
                        FontSize="Medium"
                        FontAttributes="Bold"
                        x:Name="btnIniciar"
                        Clicked="btnIniciar_Clicked" />
            </StackLayout>
        </Frame>
    </StackLayout>
</ContentPage>